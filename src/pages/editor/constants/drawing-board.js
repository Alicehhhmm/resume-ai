/**
 * ---------------------------
 * | drawing-board constants |
 * --------------------------
 */

// dpi (dots per inch)
export const DPI = 96

// Millimeters to Pixels
// 1mm ≈ 3.78px (96dpi)
export const MM_TO_PX = 3.7795275591

// Reumse page size
export const RESUME_PAGE_SIZE = {
    width: 794,
    height: 1123,
}

// Page mode scale
export const PAGE_MIN_SCALE = 0.4
export const PAGE_MAX_SCALE = 1.2
export const PAGE_INIT_SCALE = 0.6
export const PAGE_GAP = 40

export const PAGE_SIZE = {
    width: RESUME_PAGE_SIZE.width,
    height: RESUME_PAGE_SIZE.height,
}
